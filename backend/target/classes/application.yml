server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/traceability?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 1154228715
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: 1154228715
      database: 0
    jackson:
      generator:
        write-numbers-as-strings: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  jwt:
    issuer: agritrace
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+_CHARS"
    expire-minutes: 240
  qr:
    hmac-secret: "CHANGE_ME_TO_ANOTHER_LONG_RANDOM_SECRET"
  geo:
    ip2region:
      classpath: "ip2region/ip2region_v4.xdb"
  redis:
    enabled: false  # Redis，false关，true开
  file:
    storage-path: "C:/Users/Lenovo/Desktop/traceability-system/uploads"
    public-prefix: "/files"
