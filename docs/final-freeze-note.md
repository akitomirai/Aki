# 系统最终冻结说明 (Freeze Note)

本文件说明农产品全产业链溯源系统（AgriTrace）在答辩演示前的最终冻结状态。

## 1. 冻结范围
当前系统的所有核心业务模块已完成开发、测试与验证，进入冻结状态。**除非发生严重阻塞演示的 Bug，否则严禁修改以下内容：**

- **后端核心逻辑**：权限校验、JWT 过滤、操作日志切面、MyBatis-Plus 映射。
- **数据库结构**：所有表结构（`sys_user`, `trace_batch`, `trace_event`, `qr_code`, `quality_report`, `regulation_record`, `operation_log`）已冻结。
- **前端核心页面**：`BatchDetailView.vue` (管理端详情)、`TraceDetailView.vue` (追溯端详情)。
- **接口语义**：所有 `/api/enterprise/*`, `/api/platform/*`, `/api/public/*` 接口的入参和出参结构已冻结。

## 2. 固定演示账号 (演示前必查)
| 用户名 | 角色 | 密码 | 演示重点 |
| :--- | :--- | :--- | :--- |
| `testuser1` | 企业用户 | `123456` | 批次详情、模板化节点录入、参与主体查看 |
| `platform` | 平台管理员 | `123456` | 全局批次监控、日志审计 |
| `reg1` | 监管员 | `123456` | 批次监管记录查看 |

## 3. 固定演示数据 (演示前必查)
- **主追溯批次**：`BATCH20260311001` (ID: 1)
- **主追溯码 (Token)**：`test-token-2026`
- **备选追溯码**：`invalid-token` (用于演示异常流)

## 4. 固定服务端口
- **后端 (Backend)**: `8080`
- **管理端 (Admin-Web)**: `5174`
- **追溯端 (Trace-Web)**: `5173`

## 5. 建议的操作与检查顺序
1. **启动所有服务**：后端、管理端、追溯端。
2. **环境自检**：运行 `scripts/demo-precheck.ps1`。
3. **开始演示**：按照 `docs/demo-script-final.md` 顺序演示。

- **严禁**：在演示前手动在数据库中删除 ID 为 1 的批次或其关联节点。
- **建议**：若演示过程中由于多次录入导致节点过多，可使用 `testuser1` 账号录入带“演示”字样的节点以便区分。

## 6. 冻结日期
- **日期**：2026-03-14
- **状态**：**已冻结 (FINAL)**
